// Build-time constants injected by vite.config.ts's `define`. The
// values are baked at build time so we can confirm which deploy is
// live ("use version abc1234" when debugging).
declare const __PRISM_BUILD_SHA__: string;
declare const __PRISM_BUILD_TIME__: string;
